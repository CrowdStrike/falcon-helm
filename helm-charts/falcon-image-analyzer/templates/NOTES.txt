The Crowdstrike Image Analyzer Agent is now deployed to your cluster under the {{ include "falcon-image-analyzer.namespace" . }} namespace as {{ include "falcon-image-analyzer.fullname" . }}. You can now check if the agent is running by running the following command:

"kubectl -n {{ include "falcon-image-analyzer.namespace" . }} get pods"
