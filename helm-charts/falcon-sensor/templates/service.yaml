apiVersion: v1
kind: Service
metadata:
  name: {{ include "falcon-sensor.fullname" . }}
  labels:
    {{- include "falcon-sensor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "falcon-sensor.selectorLabels" . | nindent 4 }}
