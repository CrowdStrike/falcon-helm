{{ if .Values.crowdstrikeConfig.dockerAPIToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cs-image-analyzer-agent.fullname" . }}-regsecret
  labels:
  {{- include "cs-image-analyzer-agent.labels" . | nindent 4 }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "imagePullSecret" . }}
  {{- end}}